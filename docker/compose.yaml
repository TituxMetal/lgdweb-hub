services:
  lgdweb-hub:
    image: lgdweb/lgdweb-hub:prod
    container_name: lgdweb-hub
    networks:
      - reverse-proxy-nw
      - default-nw
    restart: unless-stopped

networks:
  default-nw:
    external: true
  reverse-proxy-nw:
    external: true
